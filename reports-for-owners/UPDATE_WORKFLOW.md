# Пятничный workflow обновления Google Sheet

Spreadsheet: `1je7Mz8KGfBMIi3uEvn6VjRlyP-Moy7lmqsZsYAf7xl4`

## Шаг 1: Сбор данных
```bash
cd reports-for-owners
bash build_week.sh YYYY-MM-DD YYYY-MM-DD
```
Результат: `_live_data.json` с Метрикой, Директом, ТГ.

## Шаг 2: Обновить вкладку "сайт ТГ "

### 2.1 Найти следующую колонку
Прочитать `сайт ТГ !X24` — если пусто, новая колонка = X. Если нет — Y и т.д.

### 2.2 Добавить данные (пример для колонки X)
```
batch_update на sheet "сайт ТГ ":
  B3          → "статистика по просмотрам DD.MM-DD.MM.YYYY"
  X24         → "DD-DD.MM.YY" (period label)
  X25         → leads + calls (число)
  X27         → "DD-DD.MM.YY"
  X28         → tg_clicks (число)
  X34         → "DD.MM.YYYY" (end date)
  X35         → subscribers (число)
```

### 2.3 "Что делаем" — обновить B40:B46
Очистить старые, вписать новые действия за неделю.

### 2.4 Скриншот Метрики
Chrome → `metrika.yandex.ru/stat/sources?period=YYYY-MM-DD:YYYY-MM-DD&id=104590290`
Скриншот → вставить вручную в ячейки B4:H22.

## Шаг 3: Обновить вкладку "директ"

Полная перезапись строк 4-35 данными из `_live_data.json`:

```
Строки:
  A4  → 'Кампания "Падел | Поиск" (704317567), период DD.MM.YYYY - DD.MM.YYYY'
  A5:K5 → заголовки (Всего, "", Ср.расход, Показы, Клики, CTR, Расход, Ср.цена, Глубина, Конверсия Спасибо, Конверсия Calltouch)
  A6:K6 → итоги кампании
  A8:I8 → заголовки дней
  A9:I15 → 7 дней данных

  A20 → 'Кампания "Падел | РСЯ" (704350848), период DD.MM.YYYY - DD.MM.YYYY'
  A21:K21 → заголовки
  A22:K22 → итоги
  A24:I24 → заголовки дней
  A25:I31 → 7 дней данных

  A33 → "Что делаем:"
  A34-A35 → текст
```

## Шаг 4: Обновить вкладку "реклама и продвижение соцсети"

INSERT новый блок СВЕРХУ:
1. Прочитать ВСЮ вкладку
2. Подготовить новый блок:
   ```
   Row: "DD-DD.MM.YY"
   Row: "Название1", "Описание1"
   Row: "Название2", "Описание2"
   ...
   Row: (пустая)
   Row: (пустая)
   Row: (пустая)
   ```
3. Записать новый блок + старые данные

## Шаг 5: Обновить вкладку " продвижение/мероприятия "

INSERT новый блок СВЕРХУ:
1. Прочитать ВСЮ вкладку
2. Подготовить:
   ```
   Row: "", "DD-DD.MM.YY"
   Row: "", "проведенные мероприятия"
   Row: "", "Название", "кол-во человек"
   ...
   Row: "", "запланированные мероприятия"
   Row: "", "Событие", "дата/статус"
   ...
   Row: ""  (пустая)
   ```
3. Записать новый блок + старые данные
